import { GlassEffectDirective } from './glass-effect.directive';

describe('GlassEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new GlassEffectDirective();
    expect(directive).toBeTruthy();
  });
});
